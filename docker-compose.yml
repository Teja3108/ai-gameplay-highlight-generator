name: gameplay-highlight-generator

services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    environment:
      APP_ENV: ${APP_ENV:-development}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    ports:
      - '127.0.0.1:${API_PORT:-8000}:8000'
    restart: unless-stopped
